# Protocol

`yllama-runner` uses the binary stdio frame protocol described in
[runner-protocol.md](runner-protocol.md).

The runner protocol is internal to local supervisors such as `yllmd`. It is not
a public client API.
